package com.newclient.models;

/**
 * Define a bunch of Message type associate with all type of Request/Response type that will be user in this app
 */
public enum MessageType {
	USER, POST, GROUP, LISTUSER, LISTGROUP, MESSAGEUSER, MESSAGEGROUP, QUIT, ADD, INVALID, LEAVE
}
